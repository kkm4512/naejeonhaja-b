server:
  profiles:
    active: dev,prd
  address: 0.0.0.0
  port: 9000

spring:
  mail:
    naver:
      email: ${NAVER_EMAIL}
      password: ${NAVER_PASSWORD} # 또는 앱 비밀번호
    gmail:
      email: ${GMAIL_EMAIL}
      password: ${GMAIL_PASSWORD} # 또는 앱 비밀번호
    kakao:
      email: ${KAKAO_EMAIL}
      password: ${KAKAO_PASSWORD} # 또는 앱 비밀번호

# JWT
jwt:
  secret:
    key: ${JWT_SECRET_KEY}

# Riot
riot:
  api:
    key: ${RIOT_API_KEY}

