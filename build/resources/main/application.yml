server:
  port: 8080

riot:
  api:
    key: ${RIOT_API_KEY}

springdoc:
  swagger-ui:
    path: /docs
    tags-sorter: alpha
    operations-sorter: method
    display-request-duration: true
  api-docs:
    path: /api-docs
  writer-with-default-pretty-printer: true
